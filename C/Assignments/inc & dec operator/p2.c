#include<stdio.h>

void main() {
	int a,b;
	a=6,b=9;
	int ans= --a + --a + --a + a--;

	
	printf("a= %d\n",a);
	printf("b= %d\n",b);
	printf("ans= %d\n",ans);

	ans = b-- - --a + b-- - --b + a--;

	
	printf("a= %d\n",a);
	printf("b= %d\n",b);
	printf("ans= %d\n",ans);

}
/*-------------------------------------------------------------------------------------------------------------------------------------
a=[13]				   		|  	[a]	|	
	
	ans= (--a + --a) + --a + a--		|	6	|
	   = (a   + --a) + --a + a--		|	5	|
	   = (a   +   a) + --a + a--		| 	4	|
	   = (4  +  4) +    a  + a--		| 	3 	|  //binary operates on the two operand only
	   =    8+3           + temp		|temp = 3,a=2	|

	ans=  11 + 3 =14 

---------------------------------------------------------------------------------------------------------------------------------------
a=2 , b=9					|     [a]       |               |      [b]      |  

	ans=(b-- - --a) + b-- - --b + a--	|	2	|  		|	9	|  
	   =(temp - a)  + b-- - --b + a--	| 	1	|		| temp=9,b=8	|
	   =(9 - 1)     +temp - --b + a--	|		|		| temp=8,b=7	|
	   =   8 	+  8  - --b + a--	|		|		|      	6	|	
	   =   8 	+  8  -  6  + temp	|  temp=1,a=0   |		|	6	|
	   = 8+8-6+1
	
	ans= 11
---------------------------------------------------------------------------------------------------------------------------------------
*/
