#include <stdio.h>

void main() {
      /*type: int
        this will create a storage on RAM and assign it as 20
        */
      int num=20;
      if(num >20){      //20 > 20 => false  condition false so if  block never executed
          printf(" num is greater than 20");
        }
      printf(" num : %d \n", num);        //20     
}
